// Get the protocol (http or https) of the current page
const currentUrl = window.location.protocol + "//" + window.location.host;

// Log the current URL (protocol + host) to the console
console.log("currentUrl:", currentUrl);
